#pragma once

#include <string>
#include <sstream>
#include <vector>
#include <cmath>
#include <algorithm>
#include "parser.h"

class Interpreter {
public:
    std::string Run(const std::string&);
};
